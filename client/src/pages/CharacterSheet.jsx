import React, { useEffect, useState } from "react";
import { useParams, Redirect } from "react-router-dom";
import SpellSlotDisplay from "../components/SpellSlotDisplay";
import "../App.css";
import AuthService from "../services/AuthService";
import axios from "axios";

// Namecard component

// Health bar component (can this component be flexible to be used for both HP AND TempHP?)

// Death saves component

// Hit Dice component

// Attributes component with attribute sub component

// Armor class component

// speed component

// Proficiency bonus component

/* Do AC, SPD, and Proficiency *need* components? */

// Skills button to bring up modal of skills

